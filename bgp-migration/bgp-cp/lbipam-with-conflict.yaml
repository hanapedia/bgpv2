---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: bastion-conflict
spec:
  blocks:
    - cidr: "20.1.20.0/24"
  serviceSelector:
    matchExpressions:
      - {key: metallb.universe.tf/address-pool, operator: In, values: [bastion]}
